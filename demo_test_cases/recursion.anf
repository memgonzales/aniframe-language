func factorial(x: Number) returns Number:
    if x >= 1:
        return factorial(x - 1) * x
    
    return 1

func fib(x: Number) returns Number:
    if x <= 1:
        return x 
    else:
        return fib(x - 1) + fib(x - 2)


info(fib(10))
y = factorial(5)
info(y)